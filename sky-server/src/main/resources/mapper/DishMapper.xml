<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.DishMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (id,name,image,description, category_id, price, status, create_time, update_time,create_user,update_user)
        values (#{id},#{name},#{image},#{description},#{categoryId},#{price},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <update id="update" useGeneratedKeys="true" keyProperty="id">
        update dish
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteByids">
        delete from dish where id in
        <foreach collection="ids" index="index" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>

    <select id="countById" resultType="java.lang.Integer">
        select count(*)
        from dish
        where category_id = #{categoryId}
    </select>
    <select id="pagequery" resultType="com.sky.vo.DishVO">
        select d.*,c.name as categoryName from dish d left outer join category c on d.category_id=c.id
        <where>
            <if test="name != null and name != ''">
                and d.name like concat('%',#{name},'%')
            </if>
            <if test="status != null">
                and d.status = #{status}
            </if>
            <if test="categoryId !=null">
                and d.category_id = #{categoryId}
            </if>
        </where>
    </select>
    <select id="getById" resultType="com.sky.entity.Dish">
        select * from dish where id = #{id}
    </select>
    <select id="getBySetmealId" resultType="com.sky.entity.Dish">
        select a.* from dish a left join setmeal_dish b on a.id = b.dish_id
        where b.setmeal_id = #{setmealId}
    </select>
    <select id="list" resultType="com.sky.entity.Dish">
        select * from dish
        <where>
            <if test="categoryId !=null">
                and category_id=#{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
</mapper>